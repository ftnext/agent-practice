[project]
name = "adk-workflow-samples"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.7.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.pytest]
pythonpath = ["."]
